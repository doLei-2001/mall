spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.3.106:3306/mall_pms
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml   # mapper扫描位置
  global-config:
    db-config:
      id-type: auto   # 主键自增
server:
  port: 10000
